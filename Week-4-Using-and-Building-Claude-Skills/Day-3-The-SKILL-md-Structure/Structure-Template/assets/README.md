# assets/

Shapes to match rather than rules to apply — output templates, worked examples for calibration, machine-readable config (weights, schemas). If a worked example uses tables or headings, don't wrap it in a code fence unless `SKILL.md` also says explicitly that the fence is a documentation convention for that file, not an instruction to reproduce — see the annotation in `../SKILL.md`'s Output Format section for why this matters.

Left empty in this template on purpose: Day 3's brief is the folder layout and a first-draft `SKILL.md`, not populated content. For a fully populated example of this folder in practice (a scoring config, an output template, and a worked example report), see `../../../Day-4-Build-Your-Own-Skill/cv-reviewer/assets/`.
